grid-auto-rows,
grid-auto-columns - Окончательная сетка может оказаться больше из-за элементов сетки,
                    размещенных вне явной сетки; в этом случае будут созданы неявные дорожки,
                    размер этих неявных дорожек будет определяться свойствами grid-auto-rows и grid-auto-columns.



ЛайфХак - при установке фоновой картинки для какой-либо секции (которая в теории может в будущем изменится на другую картинку) - 
такой background лучше задавать в html через тег style, для удобства редактирования при посадке страницы на CMS/




Схема - 


1.Высота header - 100vh

2.Создание overley с помощью псевдоэлемента before - фон задаётся всему блоку целиком, top 0, left 0, right - 0, bottom - 0. задаём нужное opacity,
а контенту внутри z-index 1.
3. Рамка внизу кнопок follow и go home делается с помощью псевдоэлемента after
4. Грид элементы в секции после хедера также будут выступать в роли грид контейнера,
   а центрирование текста внутри них происходит с помощью align-content: center.
5. В последней секции ready-section все грид элементы также представляют из себя грид контейнер с двумя строками, первая для картинки,
   и вторая для текста!




Для элементов в секции PORTFOLIO (идёт сразу после  хедера, устанавлиеваем высоту элементов по 32vh, самой секции высота НЕ задаётся,так же как строки,
только колонки 4, 1fr. 
Для выравнивания элементов по вертикали используем align-content: center;
Указывать имя шрифта с префиксом или суфиксом, чтобы шрифт использовался именно из проекта а не из системы








- сделать общий класс для секций с одинаковыми паддингами
- сделать класс для центрирования текста (text-center)
- и img-resonsive
- там где 1 строка или сами строки формируют высоту исходя из контента , в таком случае сроки можно не задавать явно
- в последней грид секции каждый элемент это ссылка внутри которой span как обёртка для фона картинки и ниже располагается блок div с текстом
- также в последней секции указываются только колонки для основных элементов, 4 1fr , а для внутреннего грида указывается строка 1fr 1fr




  АДАПТИВ -

1. Секция портфолию: При max-width: 992px - делаем 3 колонки высотой 40vh.
   Меньше чем 480 - делаем 1 колонку и высоту элементов 50vh.
   

2. При max-width: 768px - элементы Section Ready выстраиваются в одну колонку.


3. ФОРМА - при max-width: 768px форма раскладывает элементы на 2 фракции, а при 480 делаем уже 1fr.


Ласт секция меняется при 1200 - а именно перестраивается в 3 колонки , дальше при 992 ставим в 2 колонки , а при 768 ставим одну колонку , используем Initial